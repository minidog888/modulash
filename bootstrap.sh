#!/bin/bash
# ============================================
# Modkit Bootstrap — global/local mode detection
# ============================================

# Detect if we are in a project mode (has modulash.json or clish.build.json)